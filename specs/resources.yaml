---
  sine-data:
    format: string
    settable: false
  engine_fetch:
    format: string
    settable: true
  update_interval:
    format: string
    settable: true
  device_info:
    format: string
    settable: false
  usage_report:
    format: string
    settable: false
  fetch_status:
    format: string
    settable: false
  engine_report:
    format: string
    settable: false
